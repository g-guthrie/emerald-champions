SPECIES_ODDISH: .genderRatio = min(254, ((50 * 255) / 100)), .eggGroups = { EGG_GROUP_GRASS, DEFAULT(EGG_GROUP_GRASS, ) }, .evolutions = (const struct Evolution[]) { {EVO_LEVEL, 21, SPECIES_GLOOM}, { EVOLUTIONS_END }, },
SPECIES_GLOOM: .genderRatio = min(254, ((50 * 255) / 100)), .eggGroups = { EGG_GROUP_GRASS, DEFAULT(EGG_GROUP_GRASS, ) }, .evolutions = (const struct Evolution[]) { {EVO_ITEM, ITEM_LEAF_STONE, SPECIES_VILEPLUME} ,{EVO_ITEM, ITEM_SUN_STONE, SPECIES_BELLOSSOM}, { EVOLUTIONS_END }, },
SPECIES_BELLOSSOM: .genderRatio = min(254, ((50 * 255) / 100)), .eggGroups = { EGG_GROUP_GRASS, DEFAULT(EGG_GROUP_GRASS, ) },
SPECIES_KRICKETOT: .genderRatio = min(254, ((50 * 255) / 100)), .eggGroups = { EGG_GROUP_BUG, DEFAULT(EGG_GROUP_BUG, ) }, .evolutions = (const struct Evolution[]) { {EVO_LEVEL, 10, SPECIES_KRICKETUNE}, { EVOLUTIONS_END }, },
SPECIES_KRICKETUNE: .genderRatio = min(254, ((50 * 255) / 100)), .eggGroups = { EGG_GROUP_BUG, DEFAULT(EGG_GROUP_BUG, ) },
SPECIES_SKWOVET: .genderRatio = min(254, ((50 * 255) / 100)), .eggGroups = { EGG_GROUP_FIELD, DEFAULT(EGG_GROUP_FIELD, ) }, .evolutions = (const struct Evolution[]) { {EVO_LEVEL, 24, SPECIES_GREEDENT}, { EVOLUTIONS_END }, },
SPECIES_GREEDENT: .genderRatio = min(254, ((50 * 255) / 100)), .eggGroups = { EGG_GROUP_FIELD, DEFAULT(EGG_GROUP_FIELD, ) },
SPECIES_TAROUNTULA: .genderRatio = min(254, ((50 * 255) / 100)), .eggGroups = { EGG_GROUP_BUG, DEFAULT(EGG_GROUP_BUG, ) }, .evolutions = (const struct Evolution[]) { {EVO_LEVEL, 15, SPECIES_SPIDOPS}, { EVOLUTIONS_END }, },
SPECIES_SPIDOPS: .genderRatio = min(254, ((50 * 255) / 100)), .eggGroups = { EGG_GROUP_BUG, DEFAULT(EGG_GROUP_BUG, ) },

Reviewed src/daycare.c GetEggSpecies: walks configured evolution targets backward to the base egg species. These four retained families are breedable; this is source review, not an executed breeding session.
